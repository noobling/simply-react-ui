import React from 'react'
import HeatMap from '.'
export default {
  title: 'HeatMap',
  component: HeatMap
}

const Template = () => {
  return <HeatMap />
}

export const Default = Template.bind({})
